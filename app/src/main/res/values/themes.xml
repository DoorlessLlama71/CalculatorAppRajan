<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalculatorAppRajan" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="calculatorButtonTheme">
        <item name="android:backgroundTint">@color/lightSkyBlue</item>
        <item name="android:textColor">@color/royalBlue</item>
    </style>

    <style name="inputBoxes">
        <item name="android:hint">@string/numberInputHint</item>
        <item name="android:textColorHint">@color/lightSkyBlue</item>
        <item name="android:backgroundTint">@color/pinkFairyTale</item>
        <item name="android:layout_width">260dp</item>
        <item name = "android:layout_height">60dp</item>
        <item name = "android:textSize">24sp</item>
    </style>

    <style name="Theme.CalculatorAppRajan" parent="Base.Theme.CalculatorAppRajan" />
</resources>